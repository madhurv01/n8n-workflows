# Real secrets — never commit these
.env
.env.local

# If you ever export the workflow directly from a live n8n instance with
# "Monitor Config" already filled in, re-run `python workflow/build_workflow.py`
# to regenerate a clean, placeholder-only workflow.json before committing.

# Local reports generated by the test harness / real runs
workflow/test/*.txt
!workflow/test/sample-report.txt
*.log

# Claude Code's local tool-permission config — not part of the project
.claude/
